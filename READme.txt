# Quick Start Project
    - install python 
    - active venv for Project
    - install flask : `pip install flask`
    - install requirements : `pip install -r requirements.txt`
    - run flask : `flask run --debug`